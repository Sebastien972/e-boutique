<header>

    <div class="grey">
        <ul class="nav justify-content-end  container">
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link active text-white" aria-current="page" href="#">Héllo ! {{ app.user.firstname }}</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Déconextion</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link active text-white" aria-current="page" href="{{ path('app_register') }}">Inscription</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" id="btnConextion" href="{{ path('app_login') }}" >Conextion</a>
                </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path('admin')}}">Back-office</a>
                </li>
            {% endif %}
        </ul>
    </div>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container position-relative">
            <a class="navbar-brand position-absolute top-50 start-50 translate-middle navbar-brand" href="{{ path('home') }}">MyBoutique</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link  active" aria-current="page" href="{{ path('home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Boutique</a>
                    </li>
                </ul>
            </div>
            <a class="nav-link active text-black-50" href="{{ path('cart') }}"><i class="fas fa-cart-arrow-down"></i>Pagnier</a>
        </div>
    </nav>
</header>
    <pre>
            {{ dump(app.session) }}
        </pre>
